---
globs: *.ts,*.tsx
description: TypeScript 風格、型別與安全準則
---
### TypeScript 風格與安全

- 型別嚴格：禁止使用 `any`；公開 API 必須完整標註型別
- 錯誤優先：以防呆為先，使用 guard clause，避免過度巢狀
- 命名規範：
  - 函式使用動詞；變數使用名詞，避免縮寫
  - 以語義化名稱取代註解，提升可讀性
- 流程控制：
  - 優先早回傳，處理 edge cases 後再寫主要邏輯
  - 長表達式斷行，避免單行過長
- 註解準則：
  - 僅為複雜邏輯加上短註解，解釋「為什麼」而非「怎麼做」
  - 避免 TODO，直接實作或建立追蹤項
- 檔案風格：
  - 與既有格式一致，勿無關重排
  - 以多行取代複雜三元運算或鏈式呼叫

### 資安與 I/O

- 檔案/路徑：嚴禁未驗證的路徑拼接；使用 allowlist 或正規化
- Shell/子程序：預設禁止；必要時白名單與轉義
- IPC/序列化：驗證 schema；拒絕不必要欄位

